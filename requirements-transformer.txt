# Optional — only needed for train_transformer.py / src/models/transformer_model.py
# Install with: pip install -r requirements-transformer.txt
# (in addition to requirements.txt)
#
# A GPU is strongly recommended for fine-tuning in reasonable time, and
# internet access is required on first run to download the pretrained
# DistilBERT weights.

torch>=2.1.0
transformers>=4.35.0
datasets>=2.14.0
accelerate>=0.24.0
