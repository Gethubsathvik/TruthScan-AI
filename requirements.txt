# Core data / ML
pandas>=2.1.0
numpy>=1.26.0
# NOTE: scikit-learn >=1.10 removes PassiveAggressiveClassifier (deprecated
# in 1.8). This project uses it by name per spec, so scikit-learn is capped
# below 1.10 — see the comment in src/models/train.py for the migration path
# (SGDClassifier(loss="hinge", penalty=None, learning_rate="pa1", eta0=1.0))
# if you need a newer scikit-learn.
scikit-learn>=1.4.0,<1.10.0
scipy>=1.11.0
joblib>=1.3.2

# NLP
nltk>=3.8.1
regex>=2023.10.3

# Plotting
matplotlib>=3.8.0
seaborn>=0.13.0

# Serving
streamlit>=1.30.0
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Config / testing
python-dotenv>=1.0.0
pytest>=7.4.0
httpx>=0.26.0
